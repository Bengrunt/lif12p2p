#summary comment utiliser SVN
#labels SVN,Phase-Implementation,Help

= Introduction =
Quelques commandes en vrac svn


== Details ==

   * {{{svn update}}} : met à jours les fichiers locaux 

   * {{{svn status}}} : regarde si les fichiers sont sur le serveur 

   * {{{svn rm --force LIF12}}} : supprime le répertoire LIF12 

   * {{{svn commit -m "commentaire"}}} : met à jour le serveur 

   * {{{ svn ls .@36 }}} : affiche les fichiers de la version 36

   * {{{svn update}}} : met à jours les fichiers locaux 

   * {{{svn add socket.h}}} : ajoute socket.h sur le net

   * {{{bg}}} : si on a oublié de mettre & (par ex 'scite socket.h' ctrl-Z puis bg)

   * {{{history}}}


==Ajout fichier==

tous ce qui est en _italique_ est a remplacer

===pour se connecter===
----

   * {{{svn checkout https://lif12p2p.googlecode.com/svn/trunk/ lif12p2p --username }}}_tonidgoogle_

   * tu tape ton mot de passe (si demandé)

   * {{{cd lif12p2p}}}

===envoyer fichiers===
----

   * {{{svn add }}}_fichier_  : uniquement les fichiers locaux sont modifiés

   * {{{svn commit -m }}}"_commentaires_"   : tous les changements locaux sont envoyés

===récupèrez fichiers===
----

   * {{{svn update }}}  : récupère les fichiers qui sont sur le serveur (ça met dans ~/lif12p2p/ )

----
----
===Crédits===

Adapté du Wiki du projet lif7 [http://code.google.com/p/lifesevenpoker/ "Life Seven Poker"] par bengrunt.